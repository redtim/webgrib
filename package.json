{
  "name": "gribwebview",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "WebGL GRIB2 decoder + renderer for HRRR and friends",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.build.json && vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "maplibre-gl": "^4.7.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
